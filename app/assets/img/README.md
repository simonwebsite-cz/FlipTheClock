Drop the app logo/icon source file(s) here (e.g. `logo.png`). The whole
`assets/img/` directory is already declared in `pubspec.yaml`, so anything
placed here is bundled automatically — no pubspec edit needed.
